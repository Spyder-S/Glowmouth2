/** Production origin. Mirrors the <link rel="canonical"> in index.html. */
export const SITE_URL = 'https://glowmouth.com'

/** Where every "Join the waitlist" affordance points. There is exactly one form. */
export const WAITLIST_ANCHOR = '#waitlist'
