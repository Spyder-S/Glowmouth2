import SimplePage from '../components/SimplePage.jsx';

export default function Cookie() {
  return (
    <SimplePage title="Cookie Policy">
      <p>We use minimal cookies for session management. In fallback mode everything is
      stored locally. In a real deployment, cookies help keep you signed in securely.</p>
    </SimplePage>
  );
}
